from django.apps import AppConfig

class MembersConfig(AppConfig):
    """
    AppConfig for the Members application.
    Handles the registration and configuration of the Member custom user model.
    """
    name = 'members'